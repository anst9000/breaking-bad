import spinner from '../../img/spinner.gif'

const Spinner = () => {
  return (
    <img src={spinner} style={{ witdh: '200px', margin: 'auto', display: 'block'}} alt='loading' />
  )
}

export default Spinner
